#include<iostream>

int main (){
    using namespace std;
    float A=100; 
    float B=A; //if we call a person with second name of the person remains same.Similarly if we create float A==100 and also create float B=A means creating second name.So it means that variable and its value remains same.
    cout<<A<<endl;
    cout<<B<<endl;
    system("pause");
    return 0;
}