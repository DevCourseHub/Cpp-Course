#include<iostream>

int main(){
    using namespace std;
    int A=3;  //It is Known As variable.Variable is a container which holds values.
    cout<<A;
    //"Rules for declaring variables-"
    //"cannot exceed 255 characters"
    //"variable name can start with only alphabets or _"
    //"After the inital of variables we can put numbers or letters"
    //"Variable names are case sensitive"
    //"Variable names cannot contain special characters"
    //"Cannot use reserve Word like Return,void etc"

    system("pause");
    return 0;


}