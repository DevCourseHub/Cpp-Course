#include<iostream>

int main (){
    using namespace std;
    cout<<"Hello World!";//It is used to print words.<< is called insertion operator
    system("pause"); //we put this line to tell the compiler that the program doesnt exit until i press any key.It helps us to run the .exe file directly.Without this the .exe exits less than 1 sec and we dont see output.
    return 0; //We use return 0; to tell the cpp compiler that the main function has ended here.After C++ 11 version if we dont put return 0 at the end it automatically put there when running the program.
} 